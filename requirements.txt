ultralytics>=8.3.0
torch>=2.1.0
torchvision>=0.16.0
opencv-python>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.66.0
scipy>=1.11.0
Pillow>=10.0.0
gdown>=4.7.0

# OSNet body ReID (strongly recommended)
# Install torchreid from source:
#   pip install git+https://github.com/KaiyangZhou/deep-person-reid.git
# If that fails, the code falls back to the hand-crafted body embedding.

# Optional high-quality face embeddings:
#   pip install insightface onnxruntime-gpu
